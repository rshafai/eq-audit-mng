<c:FragmentDefinition xmlns="sap.m" xmlns:c="sap.ui.core">
  <Dialog
    id="exceptionDialog"
    title="Equipment Not in SAP"
    contentWidth="30rem">
      <content>
        <VBox class="sapUiSmallMargin">
          <Label text="Equipment Condition" class="sapUiSmallMarginTop" />
          <Select required="true"
            selectedKey="{
              path: 'exc>/eqCondition', 
              type: 'sap.ui.model.odata.type.String',
              constraints: { isDigitSequence: true, maxLength: 3 }
            }"
            items="{ path: '/EqConditionVH' }">
            <c:Item
              key="{
                path: 'Value',
                type: 'sap.ui.model.odata.type.String',
                constraints: { isDigitSequence: true, maxLength: 3 }
              }"
              text="{Text}" />
          </Select>
          <Label text="Comments" class="sapUiTinyMarginTop" />
          <TextArea
            value="{exc>/comments}"
            required="true"
            rows="5"
            width="100%"
            placeholder="Describe the equipment as much as possible." />
        </VBox>
      </content>
    <beginButton>
      <Button text="Add Exception" type="Emphasized" press=".onSaveException" />
    </beginButton>
    <endButton>
      <Button text="Cancel" press=".onCancelException" />
    </endButton>
  </Dialog>
</c:FragmentDefinition>